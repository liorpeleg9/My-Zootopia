# Zootopia with API

Generates an HTML page of animals based on user input by fetching data from the API Ninjas Animals API.

## Setup

1. Install requirements:
   ```bash
   pip3 install -r requirements.txt